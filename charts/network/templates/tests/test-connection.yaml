apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "network.fullname" . }}-test-connection"
  labels:
    {{- include "network.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "network.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
